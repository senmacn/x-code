# X API credentials (choose one auth strategy)
# Bearer token (app-only)
X_BEARER_TOKEN=""

# OAuth1.0a user context (needed to read followings)
X_API_KEY=""
X_API_SECRET=""
X_ACCESS_TOKEN=""
X_ACCESS_SECRET=""

# Proxy (will override config if set)
HTTP_PROXY="http://127.0.0.1:7890"
HTTPS_PROXY="http://127.0.0.1:7890"

# Logging
LOG_LEVEL=info